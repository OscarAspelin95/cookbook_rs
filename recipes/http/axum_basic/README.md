# Problem
You need to create a basic HTTP server to serve requests.

# Solution
Set up a basic axum HTTP server.

# Notes
Run `cargo run` and use your favorite API tool to send requests to `http://localhost:8080/health`.
