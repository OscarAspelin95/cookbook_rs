# Axum HTTP server
Simple example of using axum to create an HTTP server with a CORS layer and a single, simple route. Optionally run with Docker.

## Usage
### Docker
´make build´
´make run´

### Non-docker
´cargo run´
